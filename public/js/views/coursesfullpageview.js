var CoursesFullPageView = Backbone.View.extend({
	
	initialize: function() {
		//this.listenTo(this.collection, "reset", this.render);
	},

	render:function() {

		/*$(this.el).html(new ScheduleTableView ({
			collection: this.schedulesCollection
		}).render().el);*/

    return this;

	}

});